<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOUR IT!{{else}}DEVOURED! Eat me again!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">GROSS!</button>
</li>
